# ロードマップ — 次にやること

> 二層理論（[正本](正本_ミクロ機構.md)／[比喩](比喩_5類型マクロ.md)／[橋渡し](橋渡し_対応表.md)）を、検証と発信へ接続する作業計画。
> 推奨順序：**まず単一エージェントのシム（理論側）で妥当性を挟んでから**、多エージェント化と発信に広げる。健全性が確かめられる順。

---

## A. 理論側ロードマップ（Layer1 起点）

### A-1. ストレステスト（最優先）★ここから
既知の経営現象を取り、モデルが正しく予測するか反証を試みる。**多エージェント化の前に妥当性を確かめる**ため、まず単一エージェントのシミュレーションで挟む。

- ✅ **定理B シム**（[`sim/theorem_b_controllability.py`](../sim/theorem_b_controllability.py)）：統制可能/不能で Ĝ・行動率が **不連続 regime change**。自然回復しない無力感アトラクタ＋外生注入での脱出を再現。
- ✅ **定理A シム**（[`sim/theorem_a_reward_withdrawal.py`](../sim/theorem_a_reward_withdrawal.py)）：報酬撤回で δ がκ倍の負へ → 恐怖エンジン(τ↓) → never 条件より悪化。
- ✅ **定理C シム**（[`sim/theorem_c_undermining.py`](../sim/theorem_c_undermining.py)）：高I₀役割で `dR_eff/dx_ext<0`（顕著な金銭報酬が実効動機を下げる）。
- ✅ **定理D シム**（[`sim/theorem_d_compound_cost.py`](../sim/theorem_d_compound_cost.py)）：恐怖＝τ↓・k↑・習慣化の独立3チャネル劣化 → 局所最適ロックイン。
- → **§6 全4定理を単一エージェントで再現完了**（詳細 [sim/README.md](../sim/README.md)）。残：粛清後のリスク回避の「伝播」は多エージェント（A-3）で扱う。

### A-2. 操作化
係数群（g, τ, κ, Ĝ, k, f, I₀, w）のうち、実在組織で観測・推定できるのはどれか。理論を測定に接続する。
[比喩 §3 診断②](比喩_5類型マクロ.md) の代理指標（ミス報告率＝恐怖指数、提案数＝学習指数 …）が、どのパラメータの観測代理になるかを対応づける。

### A-3. 多エージェント層（＝「同調」「文化」の形式化）★橋の完成 — ✅ 完了
個々の π がどう集計され、規範・伝播・均衡になるか。ここで初めて「文化」を形式的に定義できる。
**[橋渡し_対応表.md](橋渡し_対応表.md) の5類型を「対応表」から「導出」へ格上げ**（各型＝集計系のアトラクタ／regime として導く）。

✅ 3結合チャネル＋統合を実装（設計＝[多エージェント設計.md](多エージェント設計.md)、詳細＝[sim/README.md](../sim/README.md)）:
- MA-1 恐怖伝播（[`sim/multiagent_fear_contagion.py`](../sim/multiagent_fear_contagion.py)）＝定理D「伝播」完成・文化的相転移。
- MA-2 社会的参照点（[`sim/multiagent_social_reference.py`](../sim/multiagent_social_reference.py)）＝相対的剥奪・不平等の純損。
- MA-3 方策模倣（[`sim/multiagent_policy_imitation.py`](../sim/multiagent_policy_imitation.py)）＝規範創発・前例主義ロックイン。
- MA-4 統合（[`sim/multiagent_archetypes.py`](../sim/multiagent_archetypes.py)）＝**(C,A) から5類型が distinct な集団 regime として創発**（§0 正直な照合付き）。
残：階層別・規模(Dunbar)・再野生化の集団版、業種別最適化（多エージェント設計 §3 の延長）。

---

## B. シミュレーション実装計画（`sim/`・別GO）

§A-1 を実装に落とす。CLAUDE.md §7 の方針に従う。

- **言語**：Python（`numpy` 中心、可視化 `matplotlib`）。Windows 前提。
- **設計**：
  - `Agent`：V, τ, k, Ĝ を保持し softmax で行動選択、マスター方程式（[正本 §5](正本_ミクロ機構.md)）で更新。
  - `Environment`：随伴構造 C を定義し R を返す（統制可能／不能を切替可能に）。
  - `run_episode()`：時系列を回す。
- **パラメータ**：設定ファイル（`config.yaml` 等）で外出し。各係数の意味をコメントで明記。
- **再現性**：乱数シード固定。
- **明記義務**：各シムが「理論のどの主張を検証しているか」をコード・README に書く。
- **粒度**：定理ごとに小さくコミット（定理B → 定理A の順）。

---

## C. 発信側ロードマップ（Layer2 起点・`site/`・別GO）

### C-1. 最小サイト（MVP）
公開できる最小：ホーム ＋ 理論ページ ＋ 記事一覧 ＋ 記事1本 ＋ About。
- 技術：Astro 既定（着手前に確定）。Tailwind。記事＝MDX、1ファイル＝1記事。
- 理論ページの元ネタ＝[比喩_5類型マクロ.md](比喩_5類型マクロ.md)。正本（式）は「深掘り」リンク先に置き、二層表記を保つ。

### C-2. 記事の作り方（比喩を薄めない）
[橋渡し §7](橋渡し_対応表.md) の検証可能な4予測を、5類型の物語で運ぶ。
- 例：「報酬の撤回はなぜ据付より危険か」＝ 定理A を ハイエナ型の燃え尽きで語る。
- 法令・数値（労基法・最低賃金 等）を扱う記事は、出典＋「変わりうる」注記を必須（CLAUDE.md §6）。

### C-3. チャネル展開
サイト＝母艦（思想の正本）。YouTube／note／SNS は外部チャネル。1コア→全展開。再生数を主指標にしない（継続して出せること・思想がストックされることが成功指標）。

---

## D. 推奨実行順（全体）

1. **融合 docs 整備** … ✅ 完了（CLAUDE.md ＋ docs 4本）。
2. **定理 A–D シム**（`sim/`）… ✅ 完了。§6 全4定理を単一エージェントで再現（[sim/README.md](../sim/README.md)）。
3. **多エージェント層（A-3）** … ✅ 完了。MA-1〜4 で結合チャネル＋(C,A)→5類型導出（橋の完成）。
4. **操作化（A-2）** … 4軸診断指標 ↔ パラメータの対応表。
5. **サイト MVP（C-1）** … ✅ 完了（`site/`・Astro静的・5ページ・完全無料）。残：記事を増やす・無料枠デプロイ。

> 単一エージェント（2）＋多エージェント／5類型導出（3）まで完了。理論コアはひと区切り。
> **残りは発信（5・本来の目的）と操作化（4）**、多エージェントの延長（階層別・規模・再野生化・業種別）。

---

## E. インフラ未整備メモ（要確認事項）

- ✅ git リポジトリ初期化済（main・初回コミットで融合docs、以降は定理ごとに小コミット）。
- `sim/` は定理A–Dの実装あり。`sim/out/` の生成図は .gitignore（再現は seed 担保）。`site/` は Astro 静的 MVP 稼働・GitHub Pages デプロイ対応済。

---

## F. コンテスト応募 — 現場どうぶつ診断（建設業向け・発信側の実プロダクト化）

> 文脈：「自動運転システム（＝AI自動化）で1円以上稼ぐ」コンテスト。評価3軸＝自律性／事業性／売上反応。
> 企画＝[コンテスト企画書.md](コンテスト企画書.md)（v2・Codexレビュー反映）、ロジック＝[診断ロジック仕様.md](診断ロジック仕様.md)。
> 位置づけ：**A-2 操作化（4軸代理指標→診断）の実装版**であり、C-3 発信チャネルの入口（無料診断→実務キット→相談）。

- ✅ **診断MVP実装**（`site/src/pages/shindan.astro` ほか）：15問→4軸健全度→Stage→7型カルテ。全クライアントJS・依存追加なし・SVGレーダー・印刷PDF。ビルド成功／ロジック5ケース／実機目視 すべてOK。
- 残（コンテスト提出まで・2〜3週間）：
  1. 型別**実務キット**（ヒヤリ朝礼キット等）の中身＝売り物の確定。
  2. **決済導線**（note / Stripe Payment Link）接続＝¥1売上。
  3. **公開デプロイ**（GitHub Pages）＋特商法・プライバシーポリシー。
  4. 建設あて**直送20件**で兆しデータ（診断完了・購入意向・相談）。
  5. デモ動画＋README（固定費0／自動化範囲／§0限界／反応実績）。
- §0厳守：「実測モデルでなく仮説生成ツール」。誇張しない。表記は「動物行動学／生態系」（"実験"は表に出さない）。
