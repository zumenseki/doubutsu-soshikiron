# --- 秘密（セキュリティ絶対ルール・絶対に追跡しない） ---
.env
.env.*
!.env.example
*.key
*.pem

# --- Python (sim/) ---
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# 生成図・出力（再現は seed で担保）
sim/out/

# --- Node / Astro (site/) ---
node_modules/
dist/
.astro/
.output/
.vercel/
.netlify/

# --- OS / エディタ ---
Thumbs.db
ehthumbs.db
.DS_Store
*.swp

# --- ローカル Claude 設定（共有しない） ---
.claude/settings.local.json
.claude/launch.json

# --- 事業戦略 doc（公開しない・shindan/コンテスト企画の内部資料） ---
# 公開 repo ゆえ、誤 add -A での流出を防ぐため追跡除外。理論 docs は公開のまま。
docs/コンテスト企画書.md
docs/診断ロジック仕様.md
docs/note_*.md
docs/直送*.md
docs/デモ*.md
docs/kit_*.md
docs/kits/
docs/提出*.md
# 統合図＝コンテスト戦略＋価格(¥500/¥980)入り。docs/img/ の理論図は公開のまま、この1本だけ除外。
docs/img/統合_診断と発信ループの循環.svg

# --- note 自動入稿ツール (ローカル専用・公開 repo に載せない) ---
note-publisher/
*vulpix-note-publisher.zip
.mcp.json
/tmp-note/
.claude/skills/note-publish/

# --- note 原稿・生成物 (公開repoゆえ追跡しない・2026-06-07) ---
# 🔴 特に note-drafts/<slug>-paid.md = 有料本文。公開repoにpushすると有料エリアが丸見え=流出。
note-drafts/
note-export/
note-images/
# 🔴 scripts/_note_json/ = note-drafts の派生JSON(本文全文＝有料テキスト含む)。同様に流出防止で追跡しない。
scripts/_note_json/

# --- Threads 集客ツール (scripts/threads/) ---
# queue.json = 投稿キューの実行状態(下書き/承認/投稿済ID)。ローカル運用＝追跡しない。
scripts/threads/queue.json
# _*.json = フック一括importのローカル作業ファイル(_hooks_import.json 等)。
scripts/threads/_*.json
# .env / .env.* は上の秘密ルールで既にグローバル無視（.env.example のみ追跡）。

# --- 動画/音声レンダー成果物（YouTube が本拠地・git 履歴肥大回避・2026-07-18） ---
*.mp4
*.wav
*.mp3
renders/

# --- AI ツール dir（公開 repo に含めない・2026-07-18） ---
.agents/
.claude/
.codex/

# --- note 運用/戦略・Threads 運用（価格/内部戦略・既存 privatize 方針の穴埋め・2026-07-18） ---
docs/note運用.md
scripts/threads/
note-embed/HANDOFF-shindan.md
