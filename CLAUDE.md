# CLAUDE.md — 動物から組織を読む二層理論

> このファイルはプロジェクトの「脳」。新セッションはまずこれを唯一の前提（ground truth）として読む。会話履歴は無い。詳細は `docs/` に分離（長すぎるCLAUDE.mdは遵守率が下がるため、本体は薄く保つ）。

---

## 1. アイデンティティ（1行）

組織を **二層** で読む理論＋発信プロジェクト。

- **Layer1（正本・ミクロ）** ＝ 動物実験ベースの形式的学習機構（恐怖・報酬・学習・随伴を方程式で記述）。
- **Layer2（比喩・マクロ）** ＝ 社会行動学ベースの5類型（オオカミ／アリ／チンパン／ボノボ／ハイエナ）。
- 両層を **多エージェント集計（＝「同調」「文化」）** で接続する。

既存事業（zumenseki / フレコン）とは完全独立のライフワーク。

---

## 2. 二層アーキテクチャ

```
 Layer2  比喩・可読・発信     5類型 / 4ダイヤル / 進化5ステージ / 階層別 / 業種別
   ▲                          → docs/比喩_5類型マクロ.md
   │
   │   橋渡し（対応表）：各類型・各ダイヤルを式パラメータへ写像
   │   ＝ A §8.1「多エージェント層 → 文化の形式的定義」の受け皿
   │                          → docs/橋渡し_対応表.md
   ▼
 Layer1  正本・厳密・機構     ΔV = η·g·(u−V) / ヒト修正項4 / 定理A–D / 恐怖の正当域
                              → docs/正本_ミクロ機構.md
```

---

## 3. 認識論的身分（§0・憲法・最優先）

これは「組織データで係数が当たっているモデル」**ではない**（当たっていない）。価値は3点：

1. **内的整合性** — 諸主張が一つの機構から矛盾なく導かれる。
2. **非自明な予測** — 通俗経営論（動機づけを1本のダイヤルと見る立場）と分岐する、検証可能な帰結を出せる。
3. **隠れた前提の可視化** — 「報酬を増やせば効く」式の暗黙仮定を、式の上で明示・反証できる。

係数は観測値でなく **機構を表現するためのパラメータ**。誇張・過剰主張は理論を弱める。

**二層表記ルール（必守）**：
- Layer1（正本）は **誇張禁止・式で語る**。
- Layer2（比喩）は **「比喩」と明示して語る**（純血種の組織は存在しない、というレンズ）。
- 比喩を式に薄めない。**比喩を式が説明する** 構図を保つ。

---

## 4. 三つのポインタ（詳細は各 doc）

- **Layer1 正本** → [docs/正本_ミクロ機構.md](docs/正本_ミクロ機構.md)
  学習＝随伴×予測誤差（Rescorla-Wagner）。恐怖と報酬は **非対称・直交の二エンジン**。ヒト修正項4（帰属ゲート g／参照点 κ,ρ／内発置換 I₀,f／象徴・遅延 x,w,k）。マスター方程式。定理A–D。恐怖の正当域。

- **Layer2 比喩** → [docs/比喩_5類型マクロ.md](docs/比喩_5類型マクロ.md)
  4ダイヤル（恐怖／報酬／学習／同調）、5類型、進化5ステージ、階層別最適化、業種別マトリクス、Edmondson 2×2、Dunbar壁、再野生化。

- **橋渡し** → [docs/橋渡し_対応表.md](docs/橋渡し_対応表.md)
  B「4ダイヤル」＝A「2エンジン＋学習＋集計」。B「恐怖⇄学習トレードオフ」＝A機構が導く帰結。5類型 ↔ (C, A, η/τ/κ/Ĝ) 対応表。Stage5硬直＝定理B（無力感アトラクタ）、再野生化＝その唯一の脱出。

---

## 5. 目的とKPI（二系統）

- **理論側**：内的整合・非自明予測・前提可視化（§3）。データ適合・再生数を主指標にしない。
- **発信側**：継続して出せる・思想がストックされる。実装は **運用軽く・壊れにくく・積み上がる** を最優先。

---

## 6. 運用ルール

- **基本は workspace `C:\Users\hayat\CLAUDE.md` を継承**（確認多めで進める・原始人モード常時ON・セキュリティ絶対ルール・並行セッションWIPボード）。重複記述しない。
- 本プロジェクト固有の追加：
  - **再現性**＝シミュレーションは乱数シード固定。
  - **法令・数値**（労基法・最低賃金 等）は出典＋「変わりうる」注記を必須。
  - **ブランド核**＝「動物・生態系の比喩」。一般的マネジメントtipsに薄めない。
  - §0（認識論的身分）を勝手に緩めない。通俗経営論へ後退しない。
  - 大きな依存追加・破壊的変更・デプロイ設定変更の前に、一度提案して確認。
  - コミットは小さく、意味のある単位で。

---

## 7. 技術スタック（初期方針）

- **シミュレーション**：Python（`numpy` 中心、可視化 `matplotlib`）。Windows前提。→ `sim/`（後フェーズ・別GO）。
- **メディアサイト**：**Astro** 既定（コンテンツ主体・高速静的・MDX）。Next.js でも可（既存スキル優先したい場合）。**着手前に確定**。→ `site/`（後フェーズ・別GO）。
- 記事＝Markdown / MDX、1ファイル＝1記事（Content Collections）。手作業コピペ運用を増やさない。

---

## 8. 構成

```
/  動物実験から学ぶマネジメント理論
├── CLAUDE.md                    ← 本ファイル（脳・薄く保つ）
├── docs/
│   ├── 正本_ミクロ機構.md        ← Layer1（形式理論本体）
│   ├── 比喩_5類型マクロ.md       ← Layer2（5類型フレームワーク本体）
│   ├── 橋渡し_対応表.md          ← Bridge（二層の写像）
│   ├── 多エージェント設計.md      ← 多エージェント層（文化の形式化）の設計
│   ├── 応用_集団蜂起.md          ← 第4の一致＝ドメイン外適用（一揆/スト/革命 ↔ 機構）
│   ├── 応用_取り付け騒ぎ.md      ← 第5の一致＝バンクラン（恐怖伝播 MA-1 の金融版・蜂起と対）
│   ├── 応用_動機の締め出し.md    ← 第6の一致＝アンダーマイニング（報酬が内発を潰す・定理C・伝播でなく置換）
│   └── ロードマップ.md           ← 次にやること
├── sim/                         ← Python シム（✅ §6全4定理 ＋ 多エージェントMA-1〜4・run_all有）
└── site/                        ← Astro 静的サイト（✅ MVP稼働・5ページ・完全無料）
```

---

## 9. 当面のフォーカス

✅ 融合docs → ✅ §6全4定理シム(A–D) → ✅ 多エージェント MA-1〜4（5類型導出＝橋の完成） → ✅ サイトMVP（site/・Astro静的・完全無料）。
**理論コアはひと区切り。** 次は：発信（記事を増やす・無料枠デプロイ）／多エージェント拡張（階層別・規模・再野生化・業種別）／操作化（測定への接続）。

詳細・進捗は [docs/ロードマップ.md](docs/ロードマップ.md)、シム一覧は [sim/README.md](sim/README.md)。

---

## 10. 文献

- **Layer1（条件づけ系）**：Pavlov / Thorndike / Skinner / Tolman / Mowrer / Rescorla & Wagner (1972) / Rescorla（随伴性ΔP）/ Schultz (1997) / Berridge / Panksepp / Seligman & Maier (1967) / Yerkes & Dodson (1908) / Arnsten / Brosnan & de Waal (2003) / Kahneman & Tversky (1979) / Deci, Koestner & Ryan (1999) / Sutton & Barto。
- **Layer2（社会性系）**：de Waal『チンパンジーの政治学』/ Dunbar数（約150）/ Edmondson（心理的安全性）/ team-of-teams（Amazon 2ピザ・Spotify squad・Haier マイクロ企業）。

> 文献の細部（巻号・ページ）は引用前に各自で確認すること。
